export default {
    rootDir: './', // The root directory to serve files from
    open: true, // Automatically open the default browser when the server starts
    watch: true, // Watch files for changes and reload the browser automatically
    appIndex: 'index.html', // The entry point for your application
    nodeResolve: true, // Enable resolving modules from node_modules
};
